<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sbeta">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SBeta">
        <activity android:name=".MapsMarkerActivity"></activity>
        <!-- API key to be removed -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="API key to be added" />

        <activity android:name=".RegisterBarCodeCountBase" />
        <activity android:name=".RegisterBarCode" />
        <activity android:name=".QRScanner" />
        <activity android:name=".GenerateCountQR" />
        <activity android:name=".GenerateBioQR" />
        <activity android:name=".TrialInfo" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".replyContent" />
        <activity android:name=".addReply" />
        <activity android:name=".showReply" />
        <activity android:name=".AddCountTrial" />
        <activity android:name=".AddBinomialTrial" />
        <activity
            android:name=".addQuestion"
            android:label="@string/title_activity_add_question"
            android:theme="@style/Theme.SBeta.NoActionBar" />
        <activity
            android:name=".showQuestion"
            android:label="@string/title_activity_show_question"
            android:theme="@style/Theme.SBeta.NoActionBar" />
        <activity android:name=".showStatsActivity" />
        <activity android:name=".TrialActivity" />
        <activity android:name=".SearchDisplay" />
        <activity android:name=".SearchUserActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".StatActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>