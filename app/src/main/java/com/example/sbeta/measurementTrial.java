package com.example.sbeta;

import android.location.Location;
public class measurementTrial extends Trial {
    public measurementTrial(double result, String participant, Location location, String trialName) {
        super(result, participant, location, trialName);
    }
}
